//"Has a functional expression called divide" That "Divides 2000 by 100"
const divide = (d) => 2000 / 100;
//"Has an arrow function called square" That "Square arrow function multiplies a number times itself" 
const square = (m) => 2 * 2;
//"Has an arrow function called add" That "Adds an arrow function that takes two parameters and adds them together"
const add = (a) => 3 + 4;